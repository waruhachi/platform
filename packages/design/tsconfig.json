{
  "extends": "@appdotbuild/typescript-config/react-library.json",
  "compilerOptions": {
    "outDir": "dist",
    "baseUrl": ".",
    "module": "preserve",
    "moduleResolution": "Bundler",
    "paths": {
      "@/*": ["*"],
      "@appdotbuild/*": ["../*"]
    }
  },
  // We skip stories for now, but we will need to fix this problem
  "exclude": ["node_modules", "dist", "**/*.stories.tsx"]
}
